{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC and directory picker only. External-app opening is deferred (see docs/Known-Limitations.md) so no opener grant is present.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
